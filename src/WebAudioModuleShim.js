// Shim removed: replaced by project-native `createInstance` implementations.
// Kept as a minimal stub to avoid accidental runtime import errors.
// File intentionally left as a removed shim indicator.
// Delete this file from your project once all imports are migrated to the
// official API. It must not provide any runtime behavior.
export default null;
