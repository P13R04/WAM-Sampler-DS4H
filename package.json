{
  "name": "wam-sampler-clean",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WAM Sampler (clean) - root package.json to start integrated server",
  "scripts": {
    "start": "node ./scripts/start-if-free.mjs",
    "dev": "node --watch server/index.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
  }
}
